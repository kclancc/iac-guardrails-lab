# Terraform state and lockfiles
*.tfstate
*.tfstate.*
*.tfvars.local
.terraform/
.terraform.lock.hcl

# FCS scan artifacts
fcs-report.sarif
fcs-report.json
pr-comment.md

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local-only demo snippets (paste into demo PRs, never committed to main)
demo-snippets/
